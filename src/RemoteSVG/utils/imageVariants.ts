type ImageVariants = {
  hover: string;
  active: string;
  disabled: string;
};
export const imageVariants: ImageVariants = {
  hover: 'hover',
  active: 'active',
  disabled: 'disabled',
};

export const DARK_MODE = 'dark';
export const LIGHT_MODE = 'light';
